{
  "name": "yarn-dedupe",
  "version": "0.1.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prepublish": "npm run clean -s && tsc -p tsconfig.json --outDir .",
    "clean": "trash '**/*.js' '!node_modules/**/*'"
  },
  "bin": "./bin/yarn-dedupe.js",
  "dependencies": {
    "@yarnpkg/lockfile": "^1.0.0",
    "semver": "^5.5.0",
    "yargs": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^10.0.3",
    "@types/semver": "^5.5.0",
    "@types/yargs": "^11.0.0",
    "trash-cli": "^1.4.0",
    "typescript": "^2.8.3"
  }
}
